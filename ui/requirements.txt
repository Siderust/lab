# Siderust Lab — GUI dependencies
streamlit>=1.30,<2
plotly>=5.18,<6
pandas>=2.1,<3
kaleido>=0.2.1              # for static PNG export in reports (optional but recommended)
streamlit-autorefresh>=1.0  # optional: auto-reload when new results appear
